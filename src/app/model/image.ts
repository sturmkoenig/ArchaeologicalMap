export interface NewImage {
  id?: number;
  name: string;
  image: string;
}
export interface ImageDB {
  id: number;
  name: string;
  imageSource: string;
}

export interface ImageEntity {
  id: number;
  name: string;
  imageSource: string;
}
